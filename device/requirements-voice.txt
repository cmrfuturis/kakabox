# Optionale Voice/ASR-Dependencies — install in den venv mit:
#   .venv/bin/pip install -r device/requirements-voice.txt
#
# Beide Backends sind optional. Pakete + Modelle nur installieren, wenn das
# Backend genutzt wird. device/voice/asr.py importiert beide lazy und wirft
# VoiceUnavailable, wenn Paket oder Modell fehlt.
#
# --- Vosk (Default, klein & schnell) ---
#   ~30 MB numpy-Wheel + ~5 MB vosk; auf Pi 5 ca. 2–3 Min Install.
vosk==0.3.45
#
# --- whisper.cpp (genauer bei Kinderstimmen/Akzent) ---
#   Baut auf Pi 5 ARM64 aus den Sources (cmake nötig: sudo apt install cmake).
#   ~3–5 Min Build, danach läuft das Modell auf CPU mit 4 Threads.
pywhispercpp==1.4.1
#
# --- Piper-TTS (Sprachausgabe: "Wie heißt dieses Lied?") ---
#   Fertiges aarch64-Wheel (kein Build), zieht onnxruntime mit (~16 MB).
#   Modell de_DE-thorsten-medium.onnx(.json) nach /usr/share/kakabox/tts/.
#   device/voice/tts.py importiert lazy → ohne Paket/Modell Fallback auf
#   espeak-ng, sonst fester Prompt. Modell-Download siehe install-wifi-onboarding.sh.
piper-tts==1.4.2
